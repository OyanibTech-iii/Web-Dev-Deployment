{% extends 'base.html.twig' %}

{% block title %}User Details - {{ user.fullName }}{% endblock %}

{% block body %}
<div class="min-h-screen bg-background py-8">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
        <!-- Header Section -->
        <div class="mb-8">
            <div class="flex items-center gap-4 mb-4">
                <a href="{{ path('app_user_index') }}" 
                   class="btn-secondary flex items-center gap-2">
                    <ion-icon name="arrow-back-outline"></ion-icon>
                    Back to Users
                </a>
            </div>
            <h1 class="text-xl font-bold text-dark-forest-green mb-2">User Details</h1>
            <p class="text-sm text-light-gray">View and manage user account information</p>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <!-- User Information Card -->
            <div class="lg:col-span-2">
                <div class="card">
                    <div class="card-header bg-gradient-to-r from-bright-green to-dark-forest-green">
                        <h2 class="text-sm font-semibold text-white flex items-center gap-2">
                            <ion-icon name="person-outline" class="text-sm"></ion-icon>
                            User Information
                        </h2>
                    </div>
                    <div class="card-body">
                        <div class="flex flex-col items-center mb-6 pb-6 border-b border-gray-100">
                            <div class="w-24 h-24 rounded-full overflow-hidden border-4 border-white shadow-md bg-gray-100 flex items-center justify-center">
                                {% if user.profileImage %}
                                    {% set profileSrc = user.profileImage %}
                                    {% if profileSrc starts with '/' %}{% set profileSrc = profileSrc[1:] %}{% endif %}
                                    <img src="{{ asset(profileSrc) }}" alt="{{ user.fullName }}" class="w-full h-full object-cover">
                                {% else %}
                                    <span class="text-3xl font-bold text-bright-green">
                                        {{ user.firstName ? user.firstName|first|upper : user.email|first|upper }}
                                    </span>
                                {% endif %}
                            </div>
                        </div>
                        <dl class="grid grid-cols-1 gap-6">
                            <div class="flex items-center justify-between py-3 border-b border-gray-200">
                                <dt class="text-xs font-medium text-gray-500">User ID</dt>
                                <dd class="text-sm font-semibold text-gray-900">#{{ user.id }}</dd>
                            </div>
                            <div class="flex items-center justify-between py-3 border-b border-gray-200">
                                <dt class="text-xs font-medium text-gray-500">Full Name</dt>
                                <dd class="text-sm font-semibold text-gray-900">{{ user.fullName }}</dd>
                            </div>
                            <div class="flex items-center justify-between py-3 border-b border-gray-200">
                                <dt class="text-xs font-medium text-gray-500">Email</dt>
                                <dd class="text-sm font-semibold text-gray-900">{{ user.email }}</dd>
                            </div>
                            <div class="flex items-center justify-between py-3 border-b border-gray-200">
                                <dt class="text-xs font-medium text-gray-500">Phone</dt>
                                <dd class="text-sm font-semibold text-gray-900">{{ user.phone ?? 'N/A' }}</dd>
                            </div>
                            <div class="flex items-center justify-between py-3 border-b border-gray-200">
                                <dt class="text-xs font-medium text-gray-500">Status</dt>
                                <dd class="text-sm font-semibold">
                                    {% if user.isActive %}
                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                            <ion-icon name="checkmark-circle" class="w-3 h-3 mr-1"></ion-icon>
                                            Active
                                        </span>
                                    {% else %}
                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-800">
                                            <ion-icon name="close-circle" class="w-3 h-3 mr-1"></ion-icon>
                                            Inactive
                                        </span>
                                    {% endif %}
                                </dd>
                            </div>
                            <div class="flex items-center justify-between py-3">
                                <dt class="text-xs font-medium text-gray-500">Roles</dt>
                                <dd class="text-sm font-semibold">
                                    <div class="flex flex-wrap gap-1">
                                        {% for role in user.roles %}
                                            {% if role != 'ROLE_USER' %}
                                                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                                                    {{ role|replace({'ROLE_': ''}) }}
                                                </span>
                                            {% endif %}
                                        {% endfor %}
                                    </div>
                                </dd>
                            </div>
                        </dl>
                    </div>
                </div>

                <!-- Account Activity -->
                <div class="card mt-6">
                    <div class="card-header bg-gradient-to-r from-blue-500 to-blue-600">
                        <h3 class="text-sm font-semibold text-white flex items-center gap-2">
                            <ion-icon name="time-outline" class="text-sm"></ion-icon>
                            Account Activity
                        </h3>
                    </div>
                    <div class="card-body">
                        <div class="space-y-4">
                            <div class="flex items-center justify-between py-3 border-b border-gray-200">
                                <dt class="text-xs font-medium text-gray-500">Created At</dt>
                                <dd class="text-sm font-semibold text-gray-900">{{ user.createdAt|date('M d, Y H:i') }}</dd>
                            </div>
                            <div class="flex items-center justify-between py-3 border-b border-gray-200">
                                <dt class="text-xs font-medium text-gray-500">Last Updated</dt>
                                <dd class="text-sm font-semibold text-gray-900">
                                    {{ user.updatedAt ? user.updatedAt|date('M d, Y H:i') : 'Never' }}
                                </dd>
                            </div>
                            <div class="flex items-center justify-between py-3">
                                <dt class="text-xs font-medium text-gray-500">Last Login</dt>
                                <dd class="text-sm font-semibold text-gray-900">
                                    {{ user.lastLoginAt ? user.lastLoginAt|date('M d, Y H:i') : 'Never' }}
                                </dd>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Actions Sidebar -->
            <div class="lg:col-span-1">
                <div class="card">
                    <div class="card-header bg-gradient-to-r from-gray-600 to-gray-700">
                        <h3 class="text-sm font-semibold text-white flex items-center gap-2">
                            <ion-icon name="settings-outline" class="text-sm"></ion-icon>
                            Actions
                        </h3>
                    </div>
                    <div class="card-body space-y-4">
                        <a href="{{ path('app_user_edit', {'id': user.id}) }}" 
                           class="btn-primary w-full flex items-center justify-center gap-2 text-sm">
                            <ion-icon name="create-outline" class="text-sm"></ion-icon>
                            Edit User
                        </a>
                        
                        <a href="{{ path('app_user_index') }}" 
                           class="btn-secondary w-full flex items-center justify-center gap-2 text-sm">
                            <ion-icon name="list-outline" class="text-sm"></ion-icon>
                            View All Users
                        </a>

                        <div class="border-t border-gray-200 pt-4">
                            <h4 class="text-xs font-medium text-gray-900 mb-3">Danger Zone</h4>
                            <button onclick="{{ user.id }}" 
                                    class="btn-danger w-full flex items-center justify-center gap-2 text-sm">
                                <ion-icon name="trash-outline" class="text-sm"></ion-icon>
                                Delete User
                            </button>
                        </div>
                    </div>
                </div>

                <!-- User Stats -->
                <div class="card mt-6">
                    <div class="card-header bg-gradient-to-r from-bright-green to-dark-forest-green">
                        <h3 class="text-sm font-semibold text-white flex items-center gap-2">
                            <ion-icon name="analytics-outline" class="text-sm"></ion-icon>
                            User Stats
                        </h3>
                    </div>
                    <div class="card-body">
                        <div class="space-y-4">
                            <div class="text-center">
                                <div class="text-lg font-bold text-bright-green">{{ user.roles|length }}</div>
                                <div class="text-xs text-gray-500">Roles Assigned</div>
                            </div>
                            <div class="text-center">
                                <div class="text-lg font-bold text-bright-green">
                                    {{ user.isActive ? 'Active' : 'Inactive' }}
                                </div>
                                <div class="text-xs text-gray-500">Account Status</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Delete Confirmation Modal -->
<div id="deleteModal" class="fixed inset-0 bg-gray-600 bg-opacity-50 overflow-y-auto h-full w-full hidden z-50">
    <div class="relative top-20 mx-auto p-5 border w-96 shadow-lg rounded-md bg-white">
        <div class="mt-3 text-center">
            <div class="mx-auto flex items-center justify-center h-12 w-12 rounded-full bg-red-100">
                <ion-icon name="warning-outline" class="h-6 w-6 text-red-600"></ion-icon>
            </div>
            <h3 class="text-lg font-medium text-gray-900 mt-2">Delete User</h3>
            <div class="mt-2 px-7 py-3">
                <p class="text-sm text-gray-500">Are you sure you want to delete this user? This action cannot be undone.</p>
            </div>
            <div class="items-center px-4 py-3">
                <form id="deleteForm" method="post" action="">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete') }}">
                    <div class="flex gap-3 justify-center">
                        <button type="button" onclick="closeDeleteModal()" 
                                class="btn-secondary px-4 py-2 text-sm">
                            Cancel
                        </button>
                        <button type="submit" 
                                class="btn-danger px-4 py-2 text-sm">
                            Delete
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<script>
// Delete confirmation
function confirmDelete(userId) {
    const modal = document.getElementById('deleteModal');
    const form = document.getElementById('deleteForm');
    form.action = '/admin/users/' + userId;
    modal.classList.remove('hidden');
}

function closeDeleteModal() {
    const modal = document.getElementById('deleteModal');
    modal.classList.add('hidden');
}

// Close modal when clicking outside
document.getElementById('deleteModal').addEventListener('click', function(e) {
    if (e.target === this) {
        closeDeleteModal();
    }
});
</script>
{% endblock %}
