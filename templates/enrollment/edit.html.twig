{% extends 'admin/base_admin.html.twig' %}

{% block title %}Edit Enrollment | Growfico{% endblock %}

{% block header_title %}
<h2 class="text-xl font-semibold text-dark-forest-green">Edit Enrollment</h2>
<p class="text-sm text-light-gray">Update enrollment details</p>
{% endblock %}

{% block admin_content %}
<div class="max-w-3xl mx-auto">
    <div class="bg-white rounded-2xl border border-gray-200 shadow-sm">
        <div class="p-6">
            {{ include('enrollment/_form.html.twig', { form: form, button_label: 'Update Enrollment' }) }}
        </div>
    </div>
</div>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Edit Enrollment{% endblock %}

{% block body %}
    <h1>Edit Enrollment</h1>

    {{ include('enrollment/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_enrollment_index') }}">back to list</a>

    {{ include('enrollment/_delete_form.html.twig') }}
{% endblock %}
